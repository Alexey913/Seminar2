Для работы в git используются следующие команды:

git version - показывает, какая версия git устанолена на компьютере;

git status - показывает текущее состояние репозитория, а также несохраненные изменения при работе с файлами;

git init - запоминаем каталог для работы;

git add file_name - добавление файла file_name в память git. Имя файла указывать с расширением;

git commit -a (-m) "..." - сохраняет текущую версию файла, в кавычках указывается комментарий;

git diff - показывает различия между последней сохранённой версией файла и текущей версией файла;

git log - статистика изменений, произведённых в файле;

git checkout name - переход к коммиту файла (name - имя из логов) или к ветке (name - имя ветки);

git branch - вывод перечня существующих веток. символом "*" при этом указана текущая ветка;

git branch new_branch - создание новой ветки с именем new_branch;

git branch -d branch_name - удаление ветки branch_name;

git log --graph - вывод существующих коммитов в графическом виде;

git merge branch_name - слияние веток, изменения из ветки branch_name вносятся в текущую ветку;

git remote add origin web-link - добавляет ссылку на удаленный репозиторий (web-link) в git;

git clone - копирование удаленного репозитория в локальный;

git pull - скачивается все актуальное содержимое из удаленного репозитория, при этом скачанное содержимое сливается с содержимым локального репозитория;

git push  - выгрузка локального репозитория на web-сервер (необходима авторизация в удаленном репозитории);

git branch -M main - указывает,какая ветка является главной в удаленном репозитории;

git push -u origin main - из ветки main удаленного репозитория загружается содержимое в локальный репозиторий (необходима авторизация в удаленном репозитории)
